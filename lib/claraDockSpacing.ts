/**
 * Abstand für die schwebende Clara-Pille: `bottom` in ClaraDock =
 * `calc(1rem + <extra> + env(safe-area-inset-bottom))`.
 * Großzügig gewählt, damit Intro-Fußleisten (Anrede, Einstieg, eID) nie überdeckt werden.
 */
export const CLARA_DOCK_EXTRA_BOTTOM_ANREDE_ENTRY = '17.75rem';
export const CLARA_DOCK_EXTRA_BOTTOM_EID = '14.25rem';
